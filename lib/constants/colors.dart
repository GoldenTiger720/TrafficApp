import 'package:flutter/material.dart';

class TrafficLightColors {
  // Bright traffic light colors for better visibility under bright street lights
  static const Color brightRed = Color(0xFFFF0000);      // Pure bright red
  static const Color brightYellow = Color(0xFFFFFF00);   // Pure bright yellow  
  static const Color brightGreen = Color(0xFF00FF00);    // Pure bright green
  
  // Alternative brighter versions with slight adjustments for even better visibility
  static const Color vividRed = Color(0xFFFF1744);       // Material red accent
  static const Color vividYellow = Color(0xFFFFD600);    // Material yellow accent
  static const Color vividGreen = Color(0xFF00E676);     // Material green accent
}